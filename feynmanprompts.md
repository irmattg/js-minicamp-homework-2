for loop - lets one run the same block of code over and over again, each time with different value
&& || ! - allows for checking if things are true or false by comparing the two 
Array - a list 
git - a version control system that lets devs push and share their code projects to Github repos via the command line
GitHub - online cloud thats hosts code files and keeps a history of them just in case individuals or teams need to go back to them. 